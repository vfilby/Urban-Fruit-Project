- title "Fruit Caches"

.row
  .span6
    - @fruit_caches.each do |fruit_cache|
      = render :partial => "snippet", :locals => { :fruit_cache => fruit_cache }

  .span6
    = gmaps( :map_options => { :container_class => "map_container", :class => "height300", :auto_adjust => true },:markers => { "data" => @google_maps_json} )
%br

= link_to 'New Fruit cache', new_fruit_cache_path
= #gmaps4rails(@google_maps_json)
